#include <stdio.h>
#include <stdlib.h>

int main()
{
    int num;
    printf("Insert a number : ");
    scanf("%d",&num);
    int i=1;
    while(i<=num)
    {
        if(i%2==1)
        {
            printf("%d ",i);
        }
        i++;
    }
    return 0;
}
