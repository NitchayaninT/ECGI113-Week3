#include <stdio.h>
#include <stdlib.h>
#include <math.h>
float trigo(int a,int b);
int main()
{
    int x,y;
    printf("Insert x,y :");
    scanf("%d,%d",&x,&y);
    printf("The hypo of %d and %d is %.2f",x,y,trigo(x,y));
    return 0;
}
float trigo(int a,int b)
{
    return sqrt(a*a+b*b);
}
